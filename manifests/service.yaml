apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-api
  labels:
    app: gitops-demo-api
spec:
  type: ClusterIP
  selector:
    app: gitops-demo-api
  ports:
    - port: 80
      targetPort: 8080
