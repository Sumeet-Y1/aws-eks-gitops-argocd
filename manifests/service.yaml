apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-api
  labels:
    app: gitops-demo-api
spec:
  type: LoadBalancer
  selector:
    app: gitops-demo-api
  ports:
    - port: 80
      targetPort: 8080
